def CalculatePi(pi, n, sign):
    for i in range(0,1000000):
        pi = pi + (sign * (4 / ((n)*(n + 1)*(n+2))))
        sign = sign * (-1)
        n += 2
    return pi

print(round(CalculatePi(3,2,1),8))
